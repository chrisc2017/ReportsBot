'''
Provides interface for user to set up a workflow (a.k.a. task). This will be the last priority.

Functions todo:
update_sharepoint_credentials_json()
select_task (pre-made excel macro, custom python script)
select_timer (date/time, repeat date/time, manually run)
select_notification ( none, local machine, webex teams, email)
'''